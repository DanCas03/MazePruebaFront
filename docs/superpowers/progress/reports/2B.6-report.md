# Task 2B.6 (fix) — Command Pattern review findings

**Fecha:** 2026-06-17
**Tipo:** fix de hallazgos de revision sobre commit ya integrado (`0750e69`).

## Hallazgos abordados

1. **IMPORTANT — patron Command roto.** `CommandInvoker` almacenaba `List<(ICommand, ArrowBoard)>` y `undo` devolvia el `boardBefore` guardado, dejando `RemoveArrowCommand.undo` como codigo muerto. Restaurado el diseno del plan: `List<ICommand>`; `undo(currentBoard)` hace pop y retorna `command.undo(currentBoard)`.
2. **IMPORTANT — test asertaba el bug.** El test pasaba el board PRE-execute a `sut.undo`. Reescrito para pasar el board POST-execute (y mutado removiendo a2) de modo que distingue delegacion real de la devolucion de un snapshot obsoleto.
3. **MINOR — RemoveArrowCommand single-use.** Anadido doc-comment que documenta el ciclo single-use y la guarda `undo`-antes-de-`execute`.

## Evidencia TDD (RED -> GREEN)

**RED** (test corregido contra el invoker bugueado):
```
Expected: [ArrowId:ArrowId(a1)]
  Actual: [ArrowId:ArrowId(a1), ArrowId:ArrowId(a2)]
   Which: at location [1] ... longer than expected
test\application\commands\command_invoker_test.dart 68:7
00:00 +3 -1: Some tests failed.
```
El invoker devolvia el snapshot pre-execute `[a1, a2]` en lugar de delegar a `command.undo(currentBoard)`, que re-anade solo `a1` sobre el tablero actual.

**GREEN** (tras reescribir `command_invoker.dart` para delegar):
```
00:00 +4: All tests passed!   (command_invoker_test.dart)
00:02 +45: All tests passed!  (suite completa)
```
`flutter analyze`: No issues found! (ran in 1.0s)

## Archivos cambiados

- `lib/application/commands/command_invoker.dart` — reescrito: `List<ICommand>`, `undo` delega al comando.
- `lib/application/commands/remove_arrow_command.dart` — doc-comment single-use; logica intacta.
- `test/application/commands/command_invoker_test.dart` — undo verifica delegacion real desde el board actual; caso "nada que deshacer".
- `AI_HISTORY.MD` — Entrada 009.

README: sin cambios (no expone internals del Command; firmas publicas `executeCommand`/`undo`/`canUndo` sin cambios).

## Self-review

- **Completeness:** los 3 hallazgos resueltos; suite completa verde (45/45); analyze limpio.
- **YAGNI:** sin abstracciones extra; el invoker solo guarda comandos.
- **Tests verifican comportamiento real:** el caso de undo construye un tablero donde el snapshot obsoleto difiere del resultado correcto (a2 removido tras execute), por lo que solo la delegacion correcta pasa — no es un test que pase por accidente. Sin mocks innecesarios (clases puras Dart).
- **Arquitectura:** capa application; domain permanece puro; sin imports de Flutter/Hive/Riverpod en application/commands. Layout D1-strict respetado (arrow_id/arrow_length desde `arrows/value_objects/`).
- **Output pristine:** sin prints; sin warnings.
