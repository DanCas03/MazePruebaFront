// lib/core/constants/durations.dart

/// Duraciones de animación compartidas entre la lógica (retraso de la victoria
/// para dejar terminar la animación) y la presentación (la propia animación).
const Duration kArrowExitDuration = Duration(milliseconds: 320);
const Duration kBlockedShakeDuration = Duration(milliseconds: 320);
